interface:
  display_name: "Gemini Share Reader"
  short_description: "Extract chats from Gemini share URLs into structured text"
  default_prompt: "Use $gemini-share-reader to read this Gemini share URL and return the conversation in markdown."
